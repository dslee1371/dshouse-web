services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      - EDITOR_TOKEN=${EDITOR_TOKEN}
      - OCI_PAR_URL=${OCI_PAR_URL}
    restart: unless-stopped
