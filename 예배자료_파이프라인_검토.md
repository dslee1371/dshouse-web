# 예배자료 파이프라인 검토

지난 5주(7/26 ~ 8/30) 동안 에디터 → 버킷 → contexts → PPT 생성을 실제로 돌리면서 부딪힌 것들을 기준으로 정리했습니다.

## 현재 흐름

```
editor.astro ──buildMaterial()──▶ "자료 - YYYYMMDD.txt" ──▶ OCI 버킷 (+ _manifest.json)
                                                                  │
                                              사용자가 수동 다운로드 ▼
                                                     contexts/ 폴더
                                                                  │
                                        Claude가 정규식으로 재파싱 ▼
                                                     worship_design.py → pptx
```

핵심 문제는 **구조화된 데이터(MaterialData)를 사람용 텍스트로 풀었다가 다시 정규식으로 읽는다**는 점입니다. 에디터가 이미 알고 있는 정보가 텍스트로 가면서 흐려지고, 매주 그 틈에서 하나씩 문제가 났습니다.

## 실제로 발생한 문제

| 주 | 원고 내용 | 결과 |
|---|---|---|
| 8/9 | `교독문 65. 시편 150편` | 65번은 시편 149편. 사용자에게 물어봐야 했음 |
| 8/9~ | `1. 1. 지존하신 주님 이름 앞에` | `numberedBlock()`이 이미 번호 있는 줄에 또 번호를 붙임 — 8/9 이후 매주 4줄씩 |
| 8/30 | `우리들의 싸울것은` | 350장 "우리들이 싸울 것은" 오타. 유사도 40점으로 겨우 매칭 |
| 매주 | `짝수절 #ffd966` → `#f7ab08` | 연회색 배경에서 대비 1.7:1. 매번 확인 후 `#C2410C`로 교체 |
| 매주 | `1절 ~ 13절` / `14~33절` / `1 ~ 11절` / `1절~13절` | 성경본문 범위 표기가 주마다 다름 |
| 매주 | 광고 5줄 평문 | 새 디자인(환영문 + 중보기도 항목 + 기도요청 안내)에 맞춰 손으로 재구성 |
| 매주 | (없음) | 인도자·헌금기도·축도 이름은 폼에 없어 "김삼열 목사"로 하드코딩 |

## 개선 제안 (우선순위순)

### 1. JSON을 함께 저장 — 효과 가장 큼, 작업량 가장 적음

`saveToBucket()`에서 txt와 같이 `자료 - YYYYMMDD.json`을 올립니다. `formData()`가 이미 그 객체입니다.

```ts
await authedFetch("/api/bucket-file", { method: "POST",
  body: JSON.stringify({ name: fileNameFor(date), content: buildMaterial(data) }) });
await authedFetch("/api/bucket-file", { method: "POST",
  body: JSON.stringify({ name: fileNameFor(date).replace(".txt", ".json"),
                         content: JSON.stringify(data, null, 2) }) });
```

txt는 사람이 읽고 주보 인쇄에 쓰고, 자동화는 JSON을 읽습니다. 정규식 파싱 자체가 없어집니다. 스킬 쪽은 `자료 - YYYYMMDD.json`이 있으면 우선 읽도록 한 줄만 바꾸면 됩니다.

### 2. 색·글자크기 기본값을 현재 디자인에 맞추기

`OPTION_DEFAULTS`가 `#ffd966`, `40`인데 실제로는 한 번도 그대로 쓰인 적이 없습니다.

```ts
export const OPTION_DEFAULTS = {
  fontSize: "44",
  oddColor: "#0F172A",     // 본문 검정
  evenColor: "#C2410C",    // 강조 주황
};
```

더 나아가 색 선택기 6개는 **디자인 시스템이 결정하는 값**이라 폼에서 빼는 게 맞습니다. 지금 규칙(홀수절 검정 / 짝수절 주황 / 마지막 절 황금 `#B8860B`)은 색 하나를 바꾸는 게 아니라 규칙이라 컬러피커로 표현이 안 됩니다. "고급" 접기 아래로 숨기거나 삭제를 권합니다.

### 3. 이중 번호 버그

```ts
function numberedBlock(text: string): string {
  return lines(text)
    .map((line) => line.replace(/^\d+\.\s*/, ""))   // ← 기존 번호 제거 후
    .map((line, index) => `${index + 1}. ${line}`)
    .join("\n");
}
```

`parseMaterialText`의 `stripNumbering`은 이미 있으니 저장 쪽만 대칭으로 맞추면 됩니다.

### 4. 자유 입력 → 선택 입력

오타·불일치가 난 항목은 전부 자유 텍스트였습니다. 데이터 소스는 이미 있으니 목록으로 바꿀 수 있습니다.

| 항목 | 지금 | 제안 | 소스 |
|---|---|---|---|
| 교독문 | `교독문 65. 시편 150편` 텍스트 | 1~137 select, 표시 "65. 시편 149편" | `parse_kyodokmun.py --list` 결과를 JSON으로 내보내 `src/data/`에 |
| 찬양 4곡 | textarea | 자동완성 (datalist) | 찬양PPT 파일명 812개 → JSON |
| 찬송가 | `43장 1절만` | 장 번호(number) + 절 선택 | — |
| 성경본문 | `고린도전서 13장 1절~13절` | 책(select) + 장 + 시작절 + 끝절 | 66권 목록 + bskorea 책코드(`1co` 등) |

성경본문이 구조화되면 `book=1co&chap=13`으로 대한성서공회 URL을 바로 만들 수 있어서 본문 확보도 자동화됩니다.

### 5. 폼에 없는 값 추가

새 디자인이 요구하는데 폼에 없는 것들입니다. 셋 다 기본값 "김삼열 목사"로 두고 바뀔 때만 고치면 됩니다.

- 인도자 (표지)
- 헌금기도
- 축도

광고는 새 디자인의 구조에 맞춰 나누는 게 좋습니다: 환영 문구(1줄) / 중보기도 대상(여러 줄) / 안내(여러 줄). 지금은 평문 5줄을 제가 매주 해석해서 배치합니다.

### 6. 수동 다운로드 단계 없애기

예배순서는 비밀 정보가 아니므로 **읽기 전용 공개 엔드포인트** 하나면 됩니다.

```
GET /api/material/latest        → 가장 최근 JSON
GET /api/material/2026-08-30    → 특정 날짜 JSON
```

토큰 없이 GET만 허용. 그러면 Claude가 `web_fetch`로 바로 가져와서 contexts 폴더 다운로드 단계가 사라집니다. (쓰기는 지금처럼 토큰 유지.)

토큰을 넘기는 방식은 권하지 않습니다 — 매번 붙여넣어야 하고, 어딘가 저장하면 그게 더 위험합니다.

### 7. 주보 순서와 PPT 순서 불일치

`toBulletin()` 주석에 "예배순서는 자료 텍스트의 순서(= PPT 슬라이드 순서)를 그대로 따릅니다"라고 돼 있지만 실제로는 다릅니다.

| | PPT | 주보(`toBulletin`) |
|---|---|---|
| 봉헌찬송 위치 | 사도신경 다음, 헌금기도 앞 | 설교 다음 |
| 사도신경·헌금기도 | 있음 | 없음 |

주보가 의도적으로 다른 것이면 주석만 고치면 되고, 같아야 하는 것이면 `order.push` 순서를 맞춰야 합니다.

### 8. 작은 것들

- `validate()`의 성경본문 정규식 `/(장|서)\s*\d+장\s*\d+절/`은 "시편 23편"을 잡지 못합니다. 4번을 하면 필요 없어집니다.
- `specialSong` 필드는 5주 내내 `remove`였습니다. 특송이 정말 가끔이면 체크박스 하나로 줄여도 됩니다.
- `_manifest.json`을 매 저장마다 읽고 다시 쓰는 구조는 동시 저장 시 유실 위험이 있습니다. 혼자 쓰시면 문제없지만, 버킷 LIST API로 대체하면 매니페스트 자체가 필요 없습니다.

## 정리

1번(JSON 저장)과 3번(이중 번호)은 각각 10줄 안쪽이고 효과가 즉시 납니다. 2번은 값 세 개 바꾸는 일입니다. 이 셋만 해도 매주 확인 질문이 거의 없어집니다. 4·5·6번은 폼 재설계라 한 번에 하는 게 좋고, 그때 스킬도 JSON 입력 기준으로 같이 바꾸겠습니다.
