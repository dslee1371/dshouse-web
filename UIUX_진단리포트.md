# 이동수하우스 홈페이지 UI/UX 진단 리포트 (v3 · 소스코드 대조 검증본)

- 분석 대상: `https://www.dsdshouse.com` (About, Services, AI 서비스, Works, 예원교회/storage, Contact 6개 페이지)
- 분석일: 2026-07-30 (v1) → 2026-07-31 (v2 도메인 재검토) → 2026-07-31 (v3 소스 검증)
- 분석 방법: v2까지는 렌더링된 페이지의 구조·콘텐츠 관찰. **v3에서는 저장소 소스코드(Astro)를 직접 대조**하여 v2의 "확인 필요" 항목을 판정하고, 관찰만으로는 보이지 않던 결함을 추가 발견.
- **한계 고지**: 픽셀 단위 색상 대비(WCAG 수치)와 실측 로딩 성능(Lighthouse)은 여전히 미측정 — 해당 항목만 "확인 필요"로 남김. 그 외 마크업·접근성·설정 항목은 모두 소스에서 판정 완료.

## v3 변경 요약

**v2에서 "확인 필요/문제"로 적었던 것 중 4건은 실제로는 이미 잘 구현되어 있었고, 대신 관찰만으로는 보이지 않던 SEO·공유 관련 결함 3건이 새로 드러났습니다.** 특히 **사이트 전체 설정이 실제 도메인과 다른 주소(`dshouse.co.kr`)를 가리키고 있어**, 애써 붙인 도메인의 SEO 효과가 나오지 않는 상태입니다. 이것이 현재 최우선 수정 대상입니다.

### ① v2 지적 중 정정 (실제로는 양호 — 조치 불필요)

| v2 지적 | 판정 | 근거 |
|---|---|---|
| Contact 폼 라벨 연결 불명확, placeholder 의존 우려 | ✅ **오지적 — 이미 양호** | 이름·이메일·문의내용 3개 필드 모두 `<label for>` ↔ `id` 정상 연결, `autocomplete`·`inputmode`까지 지정 ([contact.astro:15-30](src/pages/contact.astro:15)) |
| 제출 성공/실패 피드백 존재 여부 불명확 | ✅ **오지적 — 이미 양호** | `aria-live="polite"` 상태 영역에 전송중/성공/실패/네트워크오류 4단계 메시지, 전송 중 버튼 비활성화 ([contact.astro:56](src/pages/contact.astro:56), [66-111](src/pages/contact.astro:66)) |
| 다크모드 토글에 접근성 이름 부재 우려 | ✅ **오지적 — 이미 양호** | `aria-label="Toggle dark mode"` 지정됨 ([BaseLayout.astro:44](src/layouts/BaseLayout.astro:44)) |
| `/storage` 토큰 오류가 초기부터 노출 | ✅ **오지적** | 오류 문구는 `hidden` 상태로 시작하며 인증 실패 시에만 표시 ([storage.astro:25](src/pages/storage.astro:25)) |
| AI 서비스 `#how` 앵커 동작 확인 필요 | ✅ **정상 동작** | `id="how"` 대상 섹션 존재, `scroll-mt-24`로 헤더 가림까지 처리 ([ai-service.astro:137](src/pages/ai-service.astro:137)) |
| 이미지 alt 텍스트 확인 필요 | ✅ **해당 없음** | 사이트에 `<img>` 태그가 없음(전부 인라인 SVG). 단, 장식용 SVG에 `aria-hidden` 부여 권장(경미) |

→ **접근성·폼 사용성은 v2 우려와 달리 기본기가 잘 갖춰져 있습니다.** 이 부분은 개선 과제에서 제외합니다.

### ② v3에서 새로 발견 (관찰로는 안 보이던 결함)

| 신규 발견 | 심각도 | 내용 |
|---|:--:|---|
| **사이트 설정 도메인 불일치** | **상** | 실제 운영은 `www.dsdshouse.com`인데, 빌드 설정([astro.config.mjs:9](astro.config.mjs:9))·robots.txt·Seo 컴포넌트가 모두 `www.dshouse.co.kr`로 지정 |
| **OG(공유 미리보기) 태그 전무** | **상** | `Seo.astro`가 작성돼 있으나 **어느 페이지에서도 import되지 않아** 빌드 결과물에 `og:` 태그가 0개 |
| **favicon 404** | 중 | `<link rel="icon" href="/favicon.ico">`인데 실제 파일은 `public/favicon.svg`뿐 |
| **모바일 GNB 미대응** | 중 | `BaseLayout.astro`에 반응형 분기(`md:`)가 하나도 없음 — 6개 메뉴+토글이 항상 가로 배치 |
| **`/storage` 검색엔진 색인 허용** | 중 | 내부 운영 도구인데 `noindex` 없고 robots.txt도 `Allow: /` |
| 테마 토글 터치 타깃 미달 | 하 | 헤더 토글만 `px-2 py-1 text-xs`(약 26px) — 권장 44px 미달 |
| `ThemeToggle.astro` 컴포넌트 미사용 | 하 | 동일 로직이 BaseLayout에 인라인 중복 — 유지보수 리스크 |

### ③ v1·v2 지적 중 여전히 미해결

| 항목 | 상태 |
|---|---|
| 전체 사이트 HTTP 비암호화 | ✅ **해결**(v2) — HTTPS 정상 |
| 도메인 없이 IP:8080 노출 | ✅ **해결**(v2) |
| 개인정보처리방침 부재 | ❌ **미해결** — `src/pages/`에 `privacy` 페이지 파일 자체가 없음 |
| 회사 실체정보(사업자·주소) 부재 | ❌ **미해결** — 푸터는 저작권 한 줄뿐 ([BaseLayout.astro:70](src/layouts/BaseLayout.astro:70)) |
| 연락처가 개인 Gmail·휴대폰 | ❌ **미해결** — `qwer013777@gmail.com` / 010-6645-0137 |

---

## 1. 사전 파악 (추정)

| 항목 | 추정 내용 | 근거 |
|---|---|---|
| 서비스 유형 | 공공데이터 기반 생활·환경 IT 솔루션 **수주형 개발사 + AI SaaS 신제품** 하이브리드 | 회사 소개(About)와 "AI 동네 리포트" 제품 페이지가 공존 |
| 주요 타깃 | ①IT 개발/구축이 필요한 소규모 시설·단체(교회·주민센터·상가) ②이사·투자·시설관리 전 위험도를 알고 싶은 개인 ③지자체/관리소(B2B) | Services 패키지, AI 요금제 3종(Free/Premium/B2B) |
| 핵심 전환 목표 | **문의/상담 접수(Contact)** 가 최상위 전환. AI 서비스는 **베타 신청**이 2차 전환 | 거의 모든 CTA가 `/contact`로 수렴 |
| 특수 기능 | `/storage`(예원교회)는 일반 방문자 대상이 아닌 **토큰 인증 기반 내부 운영 도구**(예배자료→PPT 텍스트 생성/저장) | 토큰 입력 게이트 존재 |

**핵심 진단 관점**: 이 사이트는 "회사 소개 → 신뢰 형성 → 문의 전환"이 목표이므로, 아래 평가는 *방문자가 얼마나 빠르고 매끄럽게 문의까지 도달하는가*를 기준으로 합니다.

---

## 2. 종합 진단 요약

**결론: 부분 개선 필요 (Partial) — 콘텐츠·마크업 기본기는 좋으나, 사이트 설정(도메인·공유 태그)에 실질 손실이 발생 중.**

HTTPS·도메인 적용으로 v1의 최우선 신뢰 리스크는 해소됐고, 소스를 열어보니 **폼 접근성·상태 피드백·시맨틱 마크업은 v2 우려와 달리 상당히 잘 되어 있습니다.** 그러나 v3에서 드러난 문제의 성격이 바뀌었습니다: 눈에 보이는 화면은 멀쩡한데 **사이트가 자기 주소를 잘못 알고 있습니다.** 빌드 설정·사이트맵·robots.txt가 모두 실제 도메인이 아닌 `dshouse.co.kr`을 가리켜 검색엔진에 잘못된 주소를 제출하고 있고, 작성해둔 SEO 컴포넌트는 연결되지 않아 카카오톡·슬랙 등으로 링크를 공유해도 **미리보기 카드가 뜨지 않습니다.** 영업상 링크 공유가 잦은 B2B 사이트에서 이는 체감 손실이 큽니다.

남은 개선점은 세 갈래입니다: ①**사이트 설정 정합성**(도메인·OG·favicon) — 수정 비용은 작고 효과는 즉각적, ②**개인정보처리방침·회사 실체정보 부재** — 문의 폼으로 개인정보를 수집하며 외부 서비스(Formspree)로 전송까지 하고 있어 법적 리스크가 실재, ③**"IT 구축 대행" vs "AI 동네 리포트 SaaS" 정체성 이원화**로 첫 방문자 인지 부하.

---

## 3. 평가 항목별 진단

### 첫인상 · 정보구조
- **좋음**: 히어로 카피 "생활과 환경을 바꾸는 공공데이터 기반 IT"가 명확하고, 서브카피·이중 CTA(문의하기/서비스 보기) 구조가 표준적. 상단 GNB 6개 항목으로 전 페이지 접근 경로 일관.
- **문제**: 홈에서 "회사(개발 대행)"인지 "제품(AI 동네 리포트)"인지 3초 내 판별이 어려움. 히어로 바로 아래 "NEW·AI SaaS" 배너가 있어 정체성이 이원화됨 ([index.astro:25-39](src/pages/index.astro:25)). → 방문자 목표별 진입로 분기 필요.

### 시각 디자인
- **좋음**: 다크모드 토글 제공, FOUC 방지 스크립트로 새로고침 시 테마 깜빡임 차단 ([BaseLayout.astro:13-16](src/layouts/BaseLayout.astro:13)) — 세심한 처리.
- **문제**: 헤더 테마 토글만 `px-2 py-1 text-xs`로, 옆 텍스트 링크·본문 `.btn`(`px-6 py-3`)과 크기 체계가 어긋남. 정렬이 어색해 보일 근거가 실제로 있습니다.
- **확인 필요**: 색상 대비(WCAG 수치)는 미측정. 다만 `.btn-primary`의 배경 그라디언트 위 `#07263f` 텍스트, 본문의 `text-gray-500`(푸터)은 대비 미달 가능성이 있어 우선 점검 대상입니다.

### 사용성 (Usability)
- **좋음**: 문의까지 경로가 짧음(어느 페이지든 CTA 1클릭 → Contact). Services/AI 페이지에 프로세스 단계와 요금제 명시. **Contact 폼은 라벨 연결·자동완성·실시간 상태 피드백·전송 중 중복 제출 방지까지 갖춘 모범적 구현.**
- **문제**:
  - **모바일 GNB 미대응** — 헤더에 반응형 분기가 전혀 없어(`BaseLayout.astro` 전체에 `md:` 클래스 0개), 375px 화면에서 "About/Services/AI 서비스/Works/예원교회/Contact + 토글"이 한 줄에 밀집. 가로 넘침 또는 과밀로 탭 실패 가능성이 높습니다. (본문 콘텐츠는 `md:grid-cols-3` 등으로 잘 대응되어 있어, **헤더만 예외**입니다.)
  - `/storage`는 내부 도구인데 GNB에 "예원교회"로 상시 노출 + 검색엔진 색인 허용. 일반 방문자·검색 유입자가 토큰 게이트를 만나 이탈합니다.

### 반응형 · 디바이스
- **좋음**: viewport 설정 정상. 본문 레이아웃은 `md:grid-cols-3`, `flex-col md:flex-row` 등으로 모바일 세로 스택 대응 확인 — **요금제·특징 카드 3열은 모바일에서 정상 스택됩니다**(v2 확인 필요 항목 해소).
- **문제**: 위 서술대로 **헤더 GNB만 미대응**. 터치 타깃은 `.btn`이 약 44px로 기준 충족하나, 헤더 테마 토글은 약 26px로 미달.

### 성능 · 기술
- **좋음**: SSR + 주요 페이지 `prerender = true` 병용으로 정적 서빙 — 초기 로딩에 유리. 외부 이미지·폰트 의존이 없고 아이콘이 전부 인라인 SVG여서 요청 수가 매우 적음(성능상 유리). 깨진 내부 링크 미발견.
- **문제**: `favicon.ico` 요청이 매 페이지 404. `og:image`로 지정된 `/og-default.jpg`도 `public/`에 존재하지 않음.
- **확인 필요**: 실측 Lighthouse 점수·코어 웹 바이탈.

### SEO · 공유 (v3 신규 항목)
- **문제 (심각)**:
  - `astro.config.mjs`의 `site`가 `https://www.dshouse.co.kr` → 생성된 `sitemap-0.xml`의 **모든 URL이 실제 도메인이 아닌 주소**로 기록됨. 검색엔진에 존재하지 않는 주소를 제출하는 상태.
  - `public/robots.txt`의 Sitemap 경로도 동일하게 잘못된 도메인.
  - `Seo.astro`가 **미연결** → `og:title`/`og:image`/`twitter:card` 전부 부재. 카카오톡·슬랙·페이스북 공유 시 제목·썸네일 없는 맨 링크로 표시.
  - canonical 링크 부재.
- **좋음**: `<html lang="ko">`, 페이지별 `<title>`·`description` 개별 지정은 정상.

### 접근성 (a11y)
- **좋음 (v2 우려 해소)**: 폼 라벨 연결 ✅, `aria-live` 상태 알림 ✅, 토글 `aria-label` ✅, `aria-labelledby`+`sr-only` 제목으로 섹션 구조화 ✅ ([index.astro:42-43](src/pages/index.astro:42)), 허니팟에 `aria-hidden`·`tabindex="-1"` ✅. **기대 이상으로 잘 되어 있습니다.**
- **문제**: `<main id="main">`이 있는데 **스킵 링크가 없음** — 키보드 사용자가 매 페이지 GNB 7개를 통과해야 본문 도달. 장식용 인라인 SVG에 `aria-hidden="true"` 미부여.
- **확인 필요**: 색상 대비 실측.

### 콘텐츠 · 카피
- **매우 좋음**: 카피가 추상적 구호에 그치지 않고 구체적 예시("이번 주 소음 민원이 늘었어요", 캐시 전략으로 API 비용 절감 등)와 수치(요금제 금액)를 제시. Before/Now/Next 서사도 설득력 있음. 오탈자 미발견.
- **문제**: AI 서비스가 실제 가동 제품인지 "베타/컨셉"인지 모호("정식 요금제는 베타 테스트 결과에 따라 조정"). 과장 인상 방지를 위해 현재 상태(베타/준비중) 명시 권장.

### 신뢰 요소 · 법적 준수
- **취약 (핵심 약점)**:
  - 사업자등록번호·대표자·주소·개인정보처리방침 전부 부재. 푸터는 저작권 한 줄뿐.
  - **개인정보 처리 위탁 고지 누락 (v3 신규)**: 문의 폼이 이름·이메일·문의내용을 **외부 서비스 Formspree(해외)로 전송**합니다 ([contact.astro:9](src/pages/contact.astro:9)). 국내 개인정보보호법상 제3자 처리위탁·국외 이전에 해당할 수 있어, 처리방침에 위탁업체·이전국가 고지가 필요합니다. 단순 "처리방침 없음"보다 한 단계 더 무거운 리스크입니다.
  - 연락처가 개인 Gmail(`qwer013777@gmail.com`)·개인 휴대폰 → 전문성 인상 약화. 도메인 이메일 권장.

---

## 4. 문제점 표 (v3 갱신)

| 발견 문제 | 위치 | 심각도 | 사용자·비즈니스 영향 | 개선 제안 |
|---|---|:--:|---|---|
| **사이트 설정 도메인 불일치** | `astro.config.mjs`, `robots.txt`, `Seo.astro` | **상** | 사이트맵이 없는 주소를 제출 → 색인 실패, 도메인 이전 효과 상실 | 세 곳 모두 `https://www.dsdshouse.com`으로 통일 후 재빌드, Search Console 사이트맵 재제출 |
| **OG 태그 전무(Seo 컴포넌트 미연결)** | 전 페이지 | **상** | 카톡·슬랙 공유 시 썸네일·제목 없음 → 영업 링크 클릭률 저하 | `BaseLayout`에서 `Seo.astro` import, `og-default.jpg`(1200×630) 실제 추가 |
| 개인정보처리방침 부재 + **국외 위탁(Formspree) 미고지** | Contact·푸터 | **상** | 개인정보보호법 리스크, B2B·공공 대상 신뢰 실패 | `/privacy` 페이지 신설(수집항목·보유기간·위탁사 Formspree·국외이전 명시), 폼에 동의 체크박스, 푸터에 링크 |
| 회사 실체정보 부재 | 푸터 | **상** | 지자체·B2B 발주 검토 시 실체 확인 불가 | 푸터에 상호·대표자·사업자번호·주소·이메일 표기 |
| **모바일 GNB 미대응** | 헤더(전 페이지) | 중 | 모바일에서 메뉴 과밀·가로 넘침 → 탐색 실패 | 햄버거 메뉴 도입 또는 `hidden md:flex` + 모바일 드로어 |
| `/storage` 내부도구가 GNB 노출 + 색인 허용 | GNB·storage | 중 | 일반·검색 방문자가 토큰 게이트에서 이탈, 내부 도구 노출 | GNB에서 제거, `<meta name="robots" content="noindex,nofollow">` 추가, 전용 URL 배포 |
| 회사 vs 제품 정체성 이원화 | 홈 | 중 | 첫 방문자 인지 부하, 판단 지연 | 히어로에서 방문자 유형별 2트랙 진입로 분기 |
| 연락처가 개인 Gmail·휴대폰 | Contact·푸터 | 중 | 전문성/영속성 의심 | 도메인 이메일(hello@dsdshouse.com) 도입, 대표번호 별도 표기 |
| AI 서비스 실제 가동 여부 모호 | AI 서비스 | 중 | 기대-현실 괴리, 문의 후 실망 | "베타/준비중" 상태 배지 명시, 실제 리포트 예시 추가 |
| favicon 404 (`.ico` 참조 / `.svg` 존재) | 전 페이지 | 중 | 탭·북마크에 기본 아이콘 → 브랜드 인지 손실, 불필요 404 | `href="/favicon.svg" type="image/svg+xml"`로 수정 |
| 스킵 링크 부재 | 전 페이지 | 하 | 키보드 사용자가 매 페이지 GNB 7개 통과 | `<main>` 앞에 sr-only 스킵 링크 추가 |
| 테마 토글 터치 타깃 26px | 헤더 | 하 | 모바일 오터치 | 최소 44×44px 확보, `.btn` 체계와 정렬 |
| `ThemeToggle.astro` 미사용·로직 중복 | 컴포넌트 | 하 | 유지보수 시 한쪽만 수정될 위험 | 컴포넌트로 통일하거나 미사용 파일 제거 |
| 색상 대비·실측 성능 | 전 페이지 | 하(확인필요) | 미측정 | Lighthouse·대비 검사기로 실측 후 판단 |

---

## 5. 우선순위 로드맵 (v3 갱신)

**✅ 완료**: 도메인 연결 + HTTPS 적용 + HTTP→HTTPS 리다이렉트(v2). 폼 접근성·상태 피드백·본문 반응형은 **이미 구현되어 있어 과제에서 제외**(v3 검증).

**즉시 수정 (수정 비용 대비 효과 최대 — 대부분 몇 줄 변경)**
1. **도메인 정합성 통일** — `astro.config.mjs`의 `site`, `robots.txt`의 Sitemap, `Seo.astro`의 기본 url을 `https://www.dsdshouse.com`으로 교체 → 재빌드 → Search Console 재제출.
2. **`Seo.astro`를 `BaseLayout`에 연결** + `public/og-default.jpg`(1200×630) 추가 → 공유 미리보기 복구.
3. **favicon 참조를 `.svg`로 수정** (또는 `.ico` 파일 추가).
4. **개인정보처리방침 작성** — 수집항목·목적·보유기간과 함께 **Formspree 위탁·국외이전 사실 명시**, Contact 폼에 동의 체크박스, 푸터에 링크.
5. **푸터에 회사 실체정보** 표기(상호·대표자·사업자번호·주소).
6. **연락처를 도메인 이메일로 교체**, 대표번호 별도 표기.

**단기 (2~4주, 사용성·전환율)**
7. 모바일 헤더에 햄버거 메뉴 도입.
8. `/storage`를 GNB에서 분리 + `noindex` 부여, 전용 접근 경로로 전환.
9. 홈 히어로를 방문자 유형별 2트랙(구축 상담 / AI 리포트 체험)으로 재구성.
10. AI 서비스에 "베타" 상태 배지 + 실제 리포트 예시/데모 이미지 추가.

**중장기 (1~3개월, 완성도·성장)**
11. 스킵 링크·장식 SVG `aria-hidden`·색상 대비 실측 등 WCAG AA 마감(기반은 이미 양호하므로 소폭 작업).
12. Works에 실제 지표·후기·로고 등 사회적 증거 보강(현재 다소 추상적).
13. Lighthouse 실측 후 코어 웹 바이탈 개선, 애널리틱스로 문의 전환 퍼널 추적.
14. `ThemeToggle.astro` 중복 정리 등 코드 위생.

---

## 6. 잘 되어 있는 점 (유지할 강점)

- **콘텐츠 설득력**: 구호가 아닌 구체적 예시·수치·서사(Before/Now/Next)로 신뢰를 쌓는 카피 라이팅.
- **일관된 정보구조**: 6개 페이지가 동일한 GNB·푸터, 각 서비스가 특징→프로세스→요금제→CTA로 논리적 흐름.
- **명확한 전환 설계**: 모든 페이지 CTA가 문의로 수렴, 요금제·패키지를 미리 공개해 진입장벽 완화.
- **폼 구현 품질 (v3 재평가 — 강점으로 승격)**: 라벨 연결, `autocomplete`/`inputmode`, `aria-live` 상태 알림, 전송 중 버튼 비활성화, 허니팟 스팸 차단, 4단계 오류 처리까지 — 소규모 사이트 기준으로 매우 충실합니다.
- **기술 기본기**: SSR+prerender 병용, 외부 폰트·이미지 의존 없는 인라인 SVG 구성(요청 수 최소), FOUC 방지 다크모드, 본문 반응형 그리드, `lang="ko"`·페이지별 메타 지정.
- **오탈자·깨진 링크 미발견** — 기본 품질 관리가 되어 있음.

---

## 부록 · 즉시 수정 항목 체크리스트

| # | 파일 | 현재 | 변경 |
|:--:|---|---|---|
| 1 | `astro.config.mjs:9` | `site: 'https://www.dshouse.co.kr'` | `site: 'https://www.dsdshouse.com'` |
| 2 | `public/robots.txt:3` | `Sitemap: https://www.dshouse.co.kr/...` | `Sitemap: https://www.dsdshouse.com/sitemap-index.xml` |
| 3 | `src/components/Seo.astro:5` | `url = "https://www.dshouse.co.kr"` | `url = "https://www.dsdshouse.com"` |
| 4 | `src/layouts/BaseLayout.astro` | `Seo.astro` 미import | `<Seo />`를 `<head>`에 추가 |
| 5 | `src/layouts/BaseLayout.astro:12` | `href="/favicon.ico"` | `href="/favicon.svg" type="image/svg+xml"` |
| 6 | `public/` | `og-default.jpg` 없음 | 1200×630 OG 이미지 추가 |
| 7 | `src/pages/` | `privacy` 페이지 없음 | `privacy.astro` 신설 + 푸터 링크 |
| 8 | `src/pages/storage.astro` | robots 메타 없음 | `<meta name="robots" content="noindex,nofollow">` |
